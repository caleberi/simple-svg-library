{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "svg",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-Wall",
                "-g",
                "${workspaceFolder}/src/main.c",
                "${workspaceFolder}/src/svg.c",
                "-o",
                "${workspaceFolder}/build/main"
            ]
        }
    ]
}